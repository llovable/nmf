# Python 3.9: Gradio 4.44만 설치됨 (Gradio 5는 3.10+).
# Python 3.10+: Gradio 5.x가 설치됨.
# 이 검수 툴은 Gradio 4/5 API를 사용합니다.
# (Blocks에 theme/css/title, Image.select, gr.SelectData, gr.TabItem)
# Gradio 6은 theme/css가 launch()로 옮겨져 그대로 실행되지 않습니다.
#
# huggingface_hub 1.0은 HfFolder를 제거했습니다.
# Gradio 4.x는 oauth.py에서 HfFolder를 import하므로 hub 1.x와 충돌합니다.
# (Python 3.9에서 pip가 Gradio 4.44 + hub 1.x를 고르면 ImportError가 납니다)

gradio>=4.44.0,<6.0.0
huggingface_hub>=0.19.3,<1.0.0
# Pydantic 2.11+ JSON 스키마는 additionalProperties: true(bool)를 씁니다.
# Gradio 4.44(Python 3.9에서 설치됨)는 이를 dict로 처리하다 TypeError가 납니다.
pydantic>=2.0,<2.11
Pillow>=10.0.0,<13.0.0
numpy>=1.24.0,<3.0.0
shapely>=2.0.0

# 표준 라이브러리 (pip 불필요): json, glob, re, copy, shutil, os, sys, math, tkinter
# tkinter는 pip 패키지가 아님. Linux에서는 별도로 설치:
#   Ubuntu/Debian: sudo apt install python3-tk
#   Fedora: sudo dnf install python3-tkinter

# Windows EXE 패키징 (sys.frozen / PyInstaller)
pyinstaller>=6.0.0
pyinstaller-hooks-contrib>=2024.0
