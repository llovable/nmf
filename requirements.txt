# MOCHI 보고 파이프라인 (전처리·학습·평가). Python 3.10+
#
# torch는 이 파일에 넣지 않는다.
#   Mac: CUDA 없음. 전처리(fetch_brca.sh)만 이 파일로 한다. 학습 금지.
#   NVIDIA 리눅스/윈도우: PyPI CPU 휠을 쓰지 말고 pytorch.org CUDA 휠을 먼저 깐다.
#     pip install torch --index-url https://download.pytorch.org/whl/cu124
#   그 다음 pip install -r requirements.txt
#   run_next_experiments.sh 는 CUDA 없으면 종료한다.

numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
scipy>=1.11.0
matplotlib>=3.7.0

# 생존 분석(eval_survival.py)을 돌릴 때만:
# lifelines>=0.27.0
